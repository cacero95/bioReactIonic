import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.example.bio',
  appName: 'biorith',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
